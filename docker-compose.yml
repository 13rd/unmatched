version: '3.8'

services:
  unmatched-game:
    build: .
    image: docker.io/13imp/unmatched-game:0.3.9
    container_name: unmatched-game
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    networks:
      - unmatched-network

networks:
  unmatched-network:
    driver: bridge
